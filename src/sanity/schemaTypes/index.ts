import { brand } from "./brand";
import { category } from "./category";
import { order } from "./order";
import { product } from "./product";
import { promotion } from "./promotion";

export const schemaTypes = [product, category, brand, promotion, order];
